using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Coins : MonoBehaviour
{

    public int numeroDeCoins;


    // Start is called before the first frame update
    void Start()
    {
        numeroDeCoins = 0;
    }

    // Update is called once per frame
    void Update()
    {
    }
}
